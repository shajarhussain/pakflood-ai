# PakFlood AI v3 — training & preprocessing dependency contract.
# Every script in ml/training/ validates these at startup via
# real_data_contract.validate_dependencies() and fails loudly if any is missing.

# Geospatial stack
geopandas>=0.14
rasterio>=1.3
rasterstats>=0.19
shapely>=2.0
pyproj>=3.6

# Parallel / IO
joblib>=1.3
pyarrow>=15.0
tqdm>=4.66

# Data / ML
pandas>=2.1
numpy>=1.26
scikit-learn>=1.4
imbalanced-learn>=0.12
